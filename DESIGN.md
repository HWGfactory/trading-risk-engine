# DESIGN.md — Trading Risk Engine 시각 시스템

작성일 2026-09-23 · 브랜치 `redesign` · 적용 스킬 `design-taste-frontend-v1`

---

## 1. Design Read

> **Reading this as:** 규제 산업의 계측 장비를 다시 만드는 일이다. 이 도구의 상품성은 숫자를 예쁘게 보여주는 데 있지 않고 **모든 숫자가 어디서 왔는지 되짚을 수 있다**는 데 있다. 그러므로 시각 언어의 중심 소재는 색이나 곡면이 아니라 **선(hairline)과 계보(lineage)** 다. 화면은 조용하고, 대담함은 근거 추적 인터랙션 한 곳에만 쓴다.

작업 종류는 **Overhaul(리디자인)** 이다. 시각 언어는 새로 만들고 콘텐츠·기능·정보 구조·도메인 관례는 보존한다. 여기에 포트폴리오 성격의 홈을 추가한다.

청중이 둘이고 서로 요구가 다르다.

| 청중 | 성공 기준 | 설계 함의 |
|---|---|---|
| 트레이더·리스크 담당자 | 매일 빠르게 값을 넣고 읽는다 | 밀도 높게, 장식 없이, 즉각 반응 |
| 컨설팅사 채용 담당자·면접관 | 10초 안에 무엇을 하는 도구인지 안다 | 홈에서 실제 동작을 바로 보여준다 |

두 요구는 **홈에서만 랜딩 문법을 쓰고, 업무 화면은 계기판으로 남긴다**는 분리로 해소한다.

---

## 2. 스킬 적용 범위

v1 스킬에는 "Out of Scope" 절이 없고 오히려 대시보드 지침(Rule 4 Dashboard Hardening, Bento 문단, Cockpit 밀도)을 포함한다. 지시에 따라 **범위는 아래 표대로 내가 나눈다.**

| 화면 | 적용 범위 |
|---|---|
| **홈(신규)** | 스킬 전체 적용 |
| **포지션 평가 / 북 현황** | 타이포 → 간격·리듬 → 색 보정 → 모션 순서의 리디자인 레버, 인터랙션 상태(Rule 5), 폼 패턴(Rule 6), 성능 가드레일, AI Tells 금지 |
| 공통 제외 | 히어로·벤토·이미지 등 랜딩 전용 규칙은 업무 화면에 적용하지 않음 |

표 라이브러리는 추가하지 않는다. 행 수가 적고 정렬·필터 요구가 없다.

---

## 3. 스킬 규칙과 지시가 충돌한 지점

지시가 우선이라는 원칙에 따라 아래와 같이 판단했다.

| # | v1 스킬 규칙 | 지시 / 프로젝트 제약 | 판단과 이유 |
|---|---|---|---|
| 1 | "Tailwind CSS로 90% 스타일링" | 네이티브 CSS + `:root` 변수 유지 | **지시 채택.** CLAUDE.md가 Tailwind 없음을 명시하고, 토큰 한 파일 전략이 이미 서 있다. 프레임워크 교체는 리디자인의 목적이 아니다. |
| 2 | "Server Components 기본, `'use client'` 격리" | Vite SPA | **해당 없음.** 스킬도 "사용자가 다른 스택을 명시하면 예외"로 열어 둔다. |
| 3 | MOTION > 5이면 무한 반복 마이크로 애니메이션을 **넣어야 한다** | 무한 반복 애니메이션 금지 | **지시 채택.** 규제 산업에서 쉬지 않고 움직이는 화면은 신뢰를 깎는다. 덧붙여 확정 다이얼이 전부 5 이하라 이 규칙은 애초에 발동하지 않는다. |
| 4 | Bento 문단: 카드 배경 `#ffffff`, 반경 `2.5rem`, 확산 그림자 | 순수 `#ffffff` 금지, 반경 체계 6/10px, 카드는 위계 있을 때만 | **지시 채택.** 스킬 내부에서도 Rule 4(밀도 7 초과 시 카드 금지)와 이 문단이 서로 어긋난다. 밀도가 높은 제품이므로 Rule 4 쪽을 따른다. |
| 5 | 플레이스홀더로 `picsum.photos` 사용 | 무관한 스톡 이미지 금지, 실제 화면만 | **지시 채택.** 근거를 파는 제품이 가짜 이미지를 쓰면 주장과 화면이 모순된다. 아래 6-3 참조. |
| 6 | "Magnetic Button"(MOTION > 5) | — | **미적용.** 다이얼이 5 이하라 미발동이고, 계측 장비의 어휘가 아니다. |
| 7 | 기준 다이얼 8 / 6 / 4 | 화면별 지정값 | **지시 채택.** 스킬이 "사용자가 요청하면 항상 그 값을 따르라"고 스스로 규정한다. |
| 8 | Framer Motion | `motion` 패키지(`motion/react`) | **충돌 아님.** `motion`은 Framer Motion의 현재 패키지명이다. `motion/react`로 임포트한다. |

---

## 4. 현재 상태 감사

### 4-1. 현재 토큰

| 갈래 | 현재 값 | 판정 |
|---|---|---|
| 배경·면 | `--paper #eef1f4`, `--surface #ffffff` | 순수 흰색 사용 → 교체 |
| 잉크 | `--ink #13202d`, `--ink-muted #5a6878` | 대비 양호, 색조만 통일 |
| 선 | `--rule #d3dae1`, `--rule-strong #9aa7b4` | `rule-strong` 2.45:1로 WCAG 1.4.11 미달 → 교체 |
| 의미색 | `--gain #c8232c`, `--loss #1a56c7` | 관례상 보존, 채도만 보정 |
| 경고 | `--warn #a35c00`, `--warn-bg #fff5e6` | 유지·확장 |
| 포커스 | `--focus #1a56c7` | **`--loss`와 같은 값** → 포커스 링이 "매도"로 읽힘. 강조색으로 분리 |
| 오류 | `#fdeeee` / `#8a1a20` 하드코딩 | 토큰화 + 색 계열 재검토 |
| 글꼴 | IBM Plex Sans KR 4웨이트, Google Fonts `<link>` | 렌더 차단 52.6KB + 서브셋 325KB → 자체 호스팅으로 교체 |
| 크기 | `--step--1/0/1/4`, `:root { font-size: 16px }` | px 고정이 사용자 글꼴 설정을 덮어씀 → `100%` |
| 반경 | 4px(컨트롤) / 6px(패널), 문서화 없음 | 6/10px로 재정의하고 문서화 |
| z-index | **사용처 0건** | 새 체계 신설 |
| 모션 | `transition`·`animation` **0건** | 신설 |

### 4-2. 정보 구조 (보존)

```
탭 2개 (상태로 전환, URL 없음)
├── 포지션 평가 : 전표 입력 → 평가 명세(순손익 · 지표 4 · 계산 근거 7행 · 출처)
└── 북 현황     : 재평가 → 요약 4 · 포지션 표 · 상품군별 집계 · 한도 위반
```
→ 홈을 추가하고 해시 라우팅으로 주소를 부여한다. 탭 이름 "포지션 평가", "북 현황"은 그대로 둔다.

### 4-3. 살릴 패턴

- **계산 근거 표** — 제품의 정체성. 라벨 · 산식 · 금액 3열 구조와 좁은 화면 스택 배치를 그대로 승계한다.
- **매수·이익 빨강 / 매도·손실 파랑** — 한국 시장 관례. 색만 보정하고 의미는 건드리지 않는다.
- **합계 줄 이중선(`3px double`)** — 회계 관례. 새 시스템에서도 유일한 "두꺼운" 선으로 남긴다.
- **`tabular-nums` 전면 적용** — 모노 글꼴로 승격한다.
- **출처·확인일을 화면에 인용하는 습관** — 홈 증거 섹션의 소재로 재사용한다.
- **`minmax(0, 1fr)` 넘침 방어**, `pointer: coarse` 터치 분기 — 직전 패스 결과, 유지.

### 4-4. 버릴 패턴

| 버릴 것 | 이유 |
|---|---|
| 순수 `#ffffff` 면 | AI Tells |
| 포커스 링 = `--loss` 색 | 의미 충돌 |
| `.segmented { overflow: hidden }`에 잘리는 포커스 링 | WCAG 2.4.7 위반(감사 확인) |
| `role="radio"`인데 화살표키 미동작 | 약속한 ARIA 계약 미이행 |
| 오류의 빨강(`#8a1a20`) | 이 제품에서 **빨강은 매수·이익**이다. 오류에 빨강을 쓰면 의미가 겹친다 |
| 빈 값 표시 `—`(엠대시) | 금지 문자 |
| Google Fonts `<link>` | 렌더 차단 + 서드파티 |
| `:root { font-size: 16px }` | 사용자 글꼴 설정 무시 |
| `* { transition: none !important }` 전역 차단 | 의도적 감속 대안까지 죽임 |

### 4-5. 현재 다이얼 추정값

**VARIANCE 2 / MOTION 1 / DENSITY 6.** 좌우 2단 고정 그리드에 모션이 전무하고, 표는 조밀하나 카드 상자가 남아 있다.

---

## 5. 확정 다이얼

| 화면 | VARIANCE | MOTION | DENSITY | 근거 |
|---|---|---|---|---|
| **홈** | 6 | 5 | 4 | 포트폴리오 프리셋(8/6/4)에서 신뢰 우선 제약만큼 내렸다. 6이면 중앙 정렬이 금지되어 비대칭 분할이 강제되고(스킬 Rule 3), 5면 무한 반복 규칙(>5)이 발동하지 않는다. 이 두 경계가 "대담하되 들뜨지 않음"의 정확한 위치다. |
| **포지션 평가** | 4 | 4 | 7 | 입력과 결과를 나란히 읽는 화면이라 구조는 예측 가능해야 한다. 4는 비대칭 허용의 하한이라 좌우 비대칭 분할만 쓰고 그 이상은 안 쓴다. 밀도 7은 카드 금지선(7 초과) 바로 아래라, 전표 하나만 패널로 남기는 근거가 된다. |
| **북 현황** | 3 | 4 | 8 | Cockpit 대역. 카드 상자를 금지하고 1px 선으로만 데이터를 가른다. 숫자 모노 글꼴이 이 대역의 필수 규칙이다. |

세 화면의 다이얼은 다르지만 **색·글꼴·반경·모션 곡선은 하나의 시스템**을 공유한다. 다이얼은 밀도와 배치에만 작용한다.

---

## 6. 시각 시스템

### 6-1. 색

모든 값은 WCAG 계산기로 검증했다. **라이트·다크 각 21개 조합, 총 42개 전부 통과(실패 0).**

#### 라이트

| 토큰 | Hex | 용도 | 검증 |
|---|---|---|---|
| `--canvas` | `#ECEEF0` | 페이지 바탕 | — |
| `--surface` | `#F9FAFB` | 패널 | — |
| `--surface-sunken` | `#E3E6E9` | 산식 띠, 스켈레톤 | — |
| `--ink` | `#14202B` | 값·제목 | 15.81:1 / 14.20:1 |
| `--ink-muted` | `#57636F` | 라벨·보조 | 5.88:1 / 5.28:1 |
| `--rule` | `#DCE0E4` | 하이라인 | 장식선(기준 없음) |
| `--rule-strong` | `#79848F` | 입력 테두리 | 3.65:1 (1.4.11 통과) |
| `--accent` | `#0F6F66` | 링크·포커스·근거 강조 | 5.76:1 / 5.18:1 · 채도 76% |
| `--accent-soft` | `#DCEBE9` | 근거 강조 배경 | 위 글자 4.91:1 |
| `--gain` | `#BE2A31` | **매수·이익** | 5.64:1 / 5.06:1 · 채도 64% |
| `--gain-soft` | `#F7E7E8` | 이익 강조 배경 | 위 글자 4.92:1 |
| `--loss` | `#1F55B8` | **매도·손실** | 6.59:1 / 5.92:1 · 채도 71% |
| `--loss-soft` | `#E6EDF9` | 손실 강조 배경 | 위 글자 5.85:1 |
| `--alert` | `#8A5200` | 경고·오류·한도 초과 | 6.11:1 / 5.49:1 |
| `--alert-soft` | `#FBF1E0` | 경고 배경 | 위 글자 5.71:1 |

#### 다크

| 토큰 | Hex | 검증 |
|---|---|---|
| `--canvas` | `#0F1418` | 순수 검정 아님 |
| `--surface` | `#171E24` | — |
| `--surface-sunken` | `#1F272E` | — |
| `--ink` | `#E4E9ED` | 13.77:1 / 15.15:1 (순수 흰색 아님) |
| `--ink-muted` | `#9AA5B1` | 6.73:1 / 7.40:1 |
| `--rule` | `#28313A` | 장식선 |
| `--rule-strong` | `#6E7984` | 3.79:1 |
| `--accent` | `#43B3A6` | 6.60:1 / 7.27:1 · 채도 46% |
| `--accent-soft` | `#14302D` | 위 글자 5.53:1 |
| `--gain` | `#E58079` | 6.17:1 / 6.79:1 · 채도 68% |
| `--gain-soft` | `#2E1C1C` | 위 글자 5.92:1 |
| `--loss` | `#8CACDD` | 7.26:1 / 7.99:1 · 채도 54% |
| `--loss-soft` | `#17212F` | 위 글자 7.00:1 |
| `--alert` | `#D2A05A` | 7.15:1 / 7.87:1 |
| `--alert-soft` | `#2A2114` | 위 글자 6.72:1 |

#### 색 결정 세 가지

**강조색은 딥 틸 하나다.** 색상각 174°(라이트) / 173°(다크)로 `--loss` 파랑(219°)과 44° 떨어져 있고 `--gain` 빨강(357°)과는 177° 반대편이다. 채도는 76% / 46%로 80% 미만 제약을 지킨다. 보라·네온 계열은 금지 대상이라 후보에서 제외했다.

**포커스 링을 `--loss`에서 분리했다.** 현재는 포커스 색과 "매도" 색이 같은 `#1a56c7`이다. 이 제품에서 파랑은 데이터 의미색이므로 인터랙션 색으로 겸할 수 없다. 포커스는 `--accent`로 옮긴다.

**오류는 빨강을 쓰지 않는다.** 이것이 이 시스템의 가장 제품 고유한 결정이다. 일반 웹에서 오류는 빨강이지만 **여기서 빨강은 이미 매수·이익을 뜻한다.** 빨간 오류 박스는 "이익"으로 오독될 여지를 만든다. 그래서 입력 오류·한도 초과·대사 불일치를 전부 `--alert`(오커) 계열로 통일하고, 빨강·파랑은 오직 손익과 방향에만 남긴다. 구분은 색이 아니라 아이콘과 문구가 맡는다.

색각 이상 고려: 빨강↔파랑은 적록색맹(protan/deutan)에서도 보존되는 가장 안전한 쌍이다. 그럼에도 방향은 색에만 의존하지 않고 "매수"/"매도" 글자를, 손익은 부호(`+` / `−`)를 항상 함께 표기한다.

### 6-2. 글꼴

npm 레지스트리에서 후보를 실측해 정했다.

| 역할 | 글꼴 | 전달 방식 | 실측 용량 |
|---|---|---|---|
| 라틴 | **Geist** | `geist` 1.7.2, `Geist-Variable.woff2` | **68.0 KB** (100–900 전 웨이트) |
| 숫자 전용 | **Geist Mono** | 같은 패키지, `GeistMono-Variable.woff2` | **69.7 KB** (100–900) |
| 한글 | **IBM Plex Sans KR** | `@fontsource/ibm-plex-sans-kr` 5.3.0, `400.css` + `600.css` | 92개 unicode-range 서브셋, 평균 11 KB, **실사용분만 다운로드** |

```css
--font-sans: Geist, 'IBM Plex Sans KR', system-ui, sans-serif;
--font-mono: 'Geist Mono', 'IBM Plex Sans KR', ui-monospace, monospace;
```
라틴을 앞에 쌓아 영문·숫자는 Geist로, 한글은 IBM Plex Sans KR로 각각 렌더링된다.

**한글 글꼴 선택 근거 (기각 사유 포함).**

- **SUIT, Spoqa Han Sans Neo — 기각.** npm 패키지가 존재하지 않는다(`npm view` 확인). 자체 호스팅하려면 바이너리를 저장소에 직접 넣어야 해서 버전 관리가 끊긴다.
- **Wanted Sans — 기각.** `wanted-sans` 1.0.3은 존재하나 unicode-range 분할이 없어 **한글 전체를 담은 단일 파일을 항상 내려받는다.** 결정적으로 이 앱은 **KRX에서 오는 임의의 한글 종목명**을 렌더링하므로 고정 글자셋으로 서브셋을 만드는 것이 안전하지 않다. 즉 전체 한글 커버리지가 필수인데, 그 경우 용량이 지금보다 나빠진다.
- **IBM Plex Sans KR — 채택.** `@fontsource` 배포판의 `400.css`가 Google Fonts와 같은 **92개 unicode-range 서브셋** 구조를 갖는다(실측 확인). 화면에 실제로 쓰인 글자의 서브셋만 내려받으므로 임의 종목명 문제와 용량 문제를 동시에 푼다.

**한글 글꼴을 유지해도 화면 인상은 크게 바뀐다.** 이 앱에 보이는 글리프의 대부분이 숫자이기 때문이다. 숫자와 라틴을 Geist / Geist Mono로 옮기는 것만으로 지배적인 질감이 바뀐다. 한글은 조판 역할이지 주역이 아니다.

**전달 방식 개선(감사 지적 해소).** 현재는 서드파티 렌더 차단 스타일시트 52.6 KB(gzip) + 서브셋 325 KB, 두 오리진. 변경 후에는 동일 오리진, 렌더 차단 스타일시트 없음, 라틴·숫자 전 웨이트 137.7 KB 고정 + 한글 실사용분.

### 6-3. 크기 체계

`:root { font-size: 100% }` — px 고정을 제거해 사용자 브라우저 글꼴 설정을 존중한다(WCAG 1.4.4).

| 토큰 | 값 | 용도 |
|---|---|---|
| `--t-meta` | 0.75rem | 출처·확인일·단위 |
| `--t-sm` | 0.8125rem | 라벨, 산식 |
| `--t-base` | 0.9375rem | 본문, 표 |
| `--t-lg` | 1.125rem | 지표 값, 소제목 |
| `--t-xl` | 1.5rem | 섹션 제목 |
| `--t-2xl` | 2rem | 순손익 헤드라인 |
| `--t-display` | `clamp(2rem, 1.2rem + 2.6vw, 3rem)` | 홈 히어로 |

히어로는 최대 3rem에서 멈춘다. 스킬의 "Oversized H1 금지"에 따라 위계는 크기가 아니라 **굵기와 색**이 만든다. 본문 측정치는 65–75ch를 넘기지 않는다.

**숫자는 전 화면에서 `--font-mono` + `tabular-nums`.** 북 현황의 Cockpit 규칙을 만족시키면서 세 화면의 숫자 인상을 통일한다.

### 6-4. 반경

| 토큰 | 값 | 적용 대상 | 규칙 |
|---|---|---|---|
| `--r-control` | 6px | 버튼, 입력, 셀렉트, 세그먼트, 뱃지 | 손이 닿는 것 |
| `--r-panel` | 10px | 전표 패널, 명세 패널, 표 스크롤 래퍼 | 내용을 담는 것 |

**이 둘 외의 반경은 쓰지 않는다.** `0`과 `9999px`(알약)도 쓰지 않는다. 현재 전표 상단의 4px 액센트 띠는 반경과 충돌하므로(탐지기 지적) 테두리 대신 `inset` 그림자로 바꾼다.

### 6-5. z-index

현재 사용처가 0건이므로 새로 정의한다. 아래 네 단계 외의 값은 쓰지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `--z-base` | 0 | 기본 흐름 |
| `--z-sticky-cell` | 10 | 가로 스크롤 표의 고정 caption·첫 열 |
| `--z-nav` | 20 | 상단 고정 내비게이션 |
| `--z-popover` | 30 | 근거 툴팁, 테마 메뉴 |

모달과 토스트는 이번 범위에 없다. 필요해지면 40 / 50으로 확장한다.

### 6-6. 간격

4px 배수. `--sp-1` 4 · `--sp-2` 8 · `--sp-3` 12 · `--sp-4` 16 · `--sp-5` 24 · `--sp-6` 32 · `--sp-7` 48 · `--sp-8` 64 · `--sp-9` 96(홈 섹션 간격).

---

## 7. 모션

각 항목에 무엇을 전달하는지 한 문장으로 적는다. **이유를 못 대면 넣지 않는다.**

```css
--ease-out:  cubic-bezier(0.16, 1, 0.3, 1);
--ease-soft: cubic-bezier(0.65, 0, 0.35, 1);
/* 스프링(motion/react): type: "spring", stiffness: 100, damping: 20 */
```

| # | 모션 | 길이 / 곡선 | 무엇을 전달하는가 |
|---|---|---|---|
| M1 | 내비 활성 표시 이동 (`layoutId`) | 스프링 100/20 | **위계** — 지금 어느 화면에 있는지 |
| M2 | 순손익 숫자 전환 (motion value) | 320ms · `--ease-out` | **상태 전환** — 값이 방금 바뀌었다 |
| M3 | 근거 줄 ↔ 입력 칸 상호 강조 | 140ms · `--ease-out` | **위계** — 이 숫자가 어느 입력에서 나왔는가 |
| M4 | 버튼 `:active` 눌림 (`scale .98`) | 90ms · `--ease-out` | **피드백** — 눌림이 접수됐다 |
| M5 | 스켈레톤 → 결과 교체 | 200ms · opacity | **상태 전환** — 기다림이 끝났다 |
| M6 | 시세 도착 시 평가가격 칸 1회 강조 | 480ms · `--ease-soft` | **피드백** — 외부에서 값이 채워졌다 |
| M7 | 재평가된 행의 순손익 1회 강조 | 480ms · `--ease-soft` | **피드백** — 이 행이 갱신됐다 |
| M8 | 대사 상태 전환 | 180ms · `--ease-out` | **상태 전환** — 대사 중 → 일치/불일치/건너뜀 |
| M9 | 홈 히어로 근거 줄 순차 등장 | 각 260ms, stagger 80ms, 1회 | **위계** — 제품의 핵심 동작이 무엇인지 시연 |
| M10 | 홈 섹션 진입 (`whileInView`, `once`) | 300ms · `--ease-out` | **위계** — 읽는 순서를 안내 |

**공통 규칙.** `transform`과 `opacity`만 애니메이션한다. `window` 스크롤 리스너를 쓰지 않고 `whileInView`(IntersectionObserver)를 쓴다. **무한 반복 없음.** M2의 중간 프레임은 표시용이며 **마지막 프레임은 반드시 백엔드가 보낸 문자열 그대로** 렌더링한다.

`prefers-reduced-motion: reduce`에서는 M1–M10을 전부 정적 처리한다. 현재의 전역 `!important` 차단은 제거하고, 각 모션이 **상태 변화 자체는 남기고 이동만 없애는** 방식으로 대체한다(예: M7은 이동 없이 배경색 1회 표시).

---

## 8. 라이트 / 다크

- 기본은 `prefers-color-scheme`. 내비의 전환 버튼으로 수동 변경하고 선택은 `localStorage`에 저장한다.
- 구현은 `:root[data-theme="dark"]`와 `@media (prefers-color-scheme: dark) :root:not([data-theme="light"])` 두 갈래로 같은 토큰을 재정의한다. **섹션별 테마 반전은 없다.**
- `color-scheme` 속성을 선언해 셀렉트 패널·스크롤바 같은 브라우저 기본 표면까지 테마를 따르게 한다(감사 지적).
- 두 모드 모두 §6-1의 42개 대비 검증을 통과했다.

---

## 9. 화면 설계

### 9-1. 라우팅과 내비게이션

라이브러리 없이 해시 라우팅. `#/` 홈 · `#/valuation` 포지션 평가 · `#/book` 북 현황. `hashchange`로 상태를 동기화해 새로고침해도 같은 화면에 머문다.

내비게이션은 한 줄, 높이 64px(≤72px 제약). 워드마크 `PI DESK`(Geist Mono, tracking +0.08em) + 메뉴 3개 + 테마 전환. **워드마크는 지어낸 브랜드명이 아니라 실제 데스크 명칭이다.**

### 9-2. 홈 — 데스크톱 (≥1024px)

```
┌──────────────────────────────────────────────────────────────────────┐
│ PI DESK        홈   포지션 평가   북 현황                    [◐ 테마] │ 64px, --z-nav
├──────────────────────────────────────────────────────────────────────┤ 1px --rule
│                                                                      │
│  ┌── 44% ─────────────────┐   ┌── 56% ──────────────────────────┐   │ 비대칭 분할
│  │                        │   │  예시 · 시세 조회 없음           │   │ (중앙정렬 금지)
│  │  포지션 손익을          │   │ ┌─────────────────────────────┐ │   │
│  │  근거까지 계산합니다     │   │ │ 매수 │ 매도   삼성전자      │ │   │ ← 실제 컴포넌트
│  │                        │   │ │ 수량 [100]  진입 [250,000]  │ │   │   (가짜 아님)
│  │  진입가와 수량을 넣으면  │   │ │ 평가가 [277,500]            │ │   │
│  │  세금·수수료까지 반영한  │   │ ├─────────────────────────────┤ │   │
│  │  순손익과 그 산식이      │   │ │ 순손익      +2,686,588원    │ │   │ ← M2
│  │  함께 나옵니다.          │   │ │ 명목 대비        +10.75%    │ │   │
│  │                        │   │ ├─────────────────────────────┤ │   │
│  │  [포지션 평가하기]      │   │ │ 진입 명목   25,000,000      │ │   │ ← M9 순차 등장
│  │  [북 현황 보기]         │   │ │  250,000 × 100 × 1          │ │   │
│  │                        │   │ │ 수수료           7,912      │ │   │
│  │  (텍스트 요소 4개)      │   │ │ 거래세          55,500      │ │   │
│  └────────────────────────┘   │ ╞═════════════════════════════╡ │   │ ← 이중선 보존
│                               │ │ 순손익       2,686,588      │ │   │
│                               │ └─────────────────────────────┘ │   │
│                               └─────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────────────┤ 1px
│  오늘의 북                                          2026-09-23 15:59 │
│  순손익          총노출          한도 사용률        마지막 평가       │ 카드 없음,
│  +2,686,588  │  27,750,000  │  0.09% ▍           │  4분 전          │ 1px 세로선만
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌── 32% (sticky) ──┐  ┌── 68% ──────────────────────────────────┐  │ 앞 섹션과
│  │                  │  │ 소수점 오차 없는 금액 계산               │  │ 다른 계열:
│  │ 이 숫자를        │  │ 모든 금액은 Decimal로 계산하고 ...      │  │ 가로 밴드 →
│  │ 믿을 수 있는     │  ├─────────────────────────────────────────┤  │ 세로 원장
│  │ 이유             │  │ 세율과 계약명세의 출처·확인일            │  │
│  │                  │  │ 증권거래세법 시행령 · 확인 2026-09-22   │  │ ← 실제 토큰
│  │ (sticky)         │  ├─────────────────────────────────────────┤  │
│  │                  │  │ 엔진 집계와 DB 집계 자동 대사            │  │
│  │                  │  │ [일치]  ← 실제 뱃지 컴포넌트            │  │
│  │                  │  ├─────────────────────────────────────────┤  │
│  │                  │  │ 평가 이력은 수정·삭제 불가               │  │
│  │                  │  ├─────────────────────────────────────────┤  │
│  │                  │  │ 손계산 예시로 검증하는 테스트            │  │
│  │                  │  └─────────────────────────────────────────┘  │
│  └──────────────────┘                                               │
├──────────────────────────────────────────────────────────────────────┤
│  METHODOLOGY     저장소                                              │ 최소 푸터
└──────────────────────────────────────────────────────────────────────┘
```

**증거 섹션은 같은 카드 5개를 늘어놓지 않는다.** 왼쪽 고정 제목 + 오른쪽 하이라인 원장 구조이고, 각 항목의 삽화는 스톡 이미지가 아니라 **이 앱의 실제 UI 원자**(이중선, 출처 문구, 대사 뱃지)를 그대로 가져다 쓴다. 스크린샷이 필요 없고, "근거를 파는 제품이 가짜 이미지를 쓰는" 모순도 피한다. 번호 라벨과 eyebrow는 쓰지 않는다.

### 9-3. 홈 — 모바일 (<768px)

```
┌────────────────────────┐
│ PI DESK          [◐]   │  메뉴는 하단 인라인
├────────────────────────┤
│ 포지션 손익을          │  단일 열 강제
│ 근거까지 계산합니다     │
│                        │
│ 진입가와 수량을 넣으면  │
│ ... 함께 나옵니다.      │
│                        │
│ [포지션 평가하기]       │  전폭
│ [북 현황 보기]          │
├────────────────────────┤
│ 예시 · 시세 조회 없음   │
│ ┌────────────────────┐ │
│ │ 매수 │ 매도        │ │  미니 평가기는
│ │ 수량 [100]         │ │  히어로 텍스트
│ │ 진입 [250,000]     │ │  다음으로 내려감
│ │ 평가 [277,500]     │ │
│ ├────────────────────┤ │
│ │ 순손익             │ │
│ │ +2,686,588원       │ │
│ ├────────────────────┤ │
│ │ 진입 명목          │ │  근거는 라벨/금액
│ │       25,000,000   │ │  한 줄 + 산식 한 줄
│ │ 250,000 × 100 × 1  │ │  (숨기지 않음)
│ ╞════════════════════╡ │
│ │ 순손익  2,686,588  │ │
│ └────────────────────┘ │
├────────────────────────┤
│ 오늘의 북              │
│ 순손익     │ 총노출    │  2 × 2
│ +2,686,588 │27,750,000│
│ ───────────┼───────────│
│ 한도 사용률│ 마지막    │
│ 0.09% ▍    │ 4분 전    │
├────────────────────────┤
│ 이 숫자를 믿을 수      │  sticky 해제,
│ 있는 이유              │  제목이 위로
│ ───────────────────────│
│ 소수점 오차 없는 …     │
│ ───────────────────────│
│ 세율·계약명세 출처 …   │
│ ───────────────────────│
│ (이하 3개)             │
├────────────────────────┤
│ METHODOLOGY  저장소     │
└────────────────────────┘
```

### 9-4. 포지션 평가 — 데스크톱

```
┌──────────────────────────────────────────────────────────────────────┐
│ PI DESK        홈   포지션 평가   북 현황                    [◐ 테마] │
├──────────────────────────────────────────────────────────────────────┤
│ ┌─ 전표 380px ──────────┐  ┌─ 평가 명세 ──────────────────────────┐ │ VARIANCE 4:
│ │▔▔▔▔▔▔▔▔▔▔ inset 액센트 │  │                                      │ │ 좌우 비대칭
│ │  매수  │  매도         │  │  순손익                              │ │ 분할까지만
│ │  주식  │  선물         │  │  +2,686,588원      ← M2              │ │
│ │                       │  │  명목 대비 +10.75%                   │ │
│ │  종목코드              │  │                                      │ │
│ │  [005930] [시세 조회]  │  ├──────────┬──────────┬───────────────┤ │ 지표 4개는
│ │  삼성전자 277,500원    │  │ 평가 명목 │ 평가손익 │ 거래비용 │세  │ │ 1px 세로선
│ │  2026-09-22 · pykrx   │  ├──────────┴──────────┴───────────────┤ │
│ │                       │  │  계산 근거                           │ │
│ │  수량      진입가      │  │  진입 명목금액   250,000 × 100 × 1   │ │
│ │  [100]     [250,000]  │  │                       25,000,000     │ │ ← M3 상호 강조
│ │       ↕ M3            │  │  수수료          진입 … × 0.015%     │ │   (--accent-soft)
│ │  평가가격              │  │                            7,912     │ │
│ │  [277,500]  ← M6      │  │  거래세 (KOSPI)  청산 매도 대금 …    │ │
│ │                       │  │                           55,500     │ │
│ │  수수료율 (%)          │  │ ╞════════════════════════════════╡   │ │ 이중선 보존
│ │  [0.015]              │  │  순손익               2,686,588     │ │
│ │                       │  │                                      │ │
│ │  [평가하기] [북에 추가]│  │  거래승수 1 · KOSPI 거래세 0.2%      │ │
│ └───────────────────────┘  │  확인일 2026-09-22                   │ │
│                            └──────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
      입력 유효 시 300ms 디바운스로 자동 평가 · [평가하기]는 명시적 재평가
```

<768px에서는 단일 열, 전표 → 명세 순. 계산 근거는 직전 패스에서 만든 `[라벨 | 금액] + [산식 전폭]` 2줄 격자를 그대로 쓴다.

### 9-5. 북 현황 — 데스크톱 (DENSITY 8, 카드 금지)

```
┌──────────────────────────────────────────────────────────────────────┐
│ PI DESK        홈   포지션 평가   북 현황                    [◐ 테마] │
├──────────────────────────────────────────────────────────────────────┤
│ 주식은 저장된 종목코드로 종가를 조회합니다        [전체 재평가]       │
├────────────┬────────────┬────────────┬───────────────────────────────┤ 카드 상자 없음
│ 총노출     │ 순노출     │ 순손익     │ 엔진·SQL 대사                 │ 1px 선만
│ 27,750,000 │+27,750,000 │ +2,686,588 │ 일치        ← M8              │
├────────────┴────────────┴────────────┴───────────────────────────────┤
│ 종목      방향  수량   진입가    평가가격   순손익     비용  평가 기준│ 숫자 전부
│ ─────────────────────────────────────────────────────────────────────│ Geist Mono
│ 삼성전자   매수   100  250,000   277,500  +2,686,588 63,412 2026-09-22│ tabular-nums
│ 주식 005930                                     ↑ M7          pykrx  │
│ ─────────────────────────────────────────────────────────────────────│
│ 상품군별 집계 (SQL 뷰 v_book_by_asset_class와 같은 기준)              │
│ 주식        1   27,750,000  +27,750,000  +2,686,588                  │
└──────────────────────────────────────────────────────────────────────┘
```

<768px에서는 두 표 모두 가로 스크롤 컨테이너 안에 두고 caption을 `left: 0`으로 고정한다(직전 패스 결과 유지). 요약 4개는 2 × 2 하이라인 격자.

---

## 10. 백엔드 변경 (CLAUDE.md 규칙 준수)

프론트는 계산하지 않는다. 아래 두 값은 **엔진이 Decimal로 계산해 문자열로 내려준다.**

### 10-1. `TraceStep.inputs` — 근거 추적(M3)의 데이터 원천

`linear.py`의 `TraceStep`에 `inputs: tuple[str, ...]`를 추가한다. **의존 관계는 엔진이 알려주고 프론트는 추측하지 않는다.**

| 근거 줄 | `inputs` |
|---|---|
| `entry_notional` | `entry_price`, `quantity`, `multiplier` |
| `mark_notional` | `mark_price`, `quantity`, `multiplier` |
| `gross_pnl` | `entry_price`, `mark_price`, `quantity`, `multiplier`, `direction` |
| `commission` | `entry_price`, `mark_price`, `quantity`, `multiplier`, `commission_rate` |
| `transaction_tax` (주식) | `quantity`, `multiplier`, `direction`, `market`, `tax_rate` + 매수는 `mark_price` / 매도는 `entry_price` |
| `transaction_tax` (선물) | `asset_class` |
| `net_pnl` | 위 전부 |
| `return_on_notional` | 위 전부 + `entry_price` |
| `initial_margin` | `entry_price`, `quantity`, `multiplier`, `margin_rate` |
| `return_on_margin` | 위 전부 + `margin_rate` |

식별자는 전표 입력 필드명과 설정값 키(`multiplier`, `tax_rate`)를 쓴다. 프론트는 이 문자열을 DOM 대상에 매핑만 한다.

함께 고칠 것: `schemas.py`(`TraceStepOut.inputs`), `types.ts`, `METHODOLOGY.md`, `tests/test_linear.py`(각 단계의 `inputs` 기대값 테스트).

### 10-2. 한도 사용률과 마지막 평가 시각 — 홈 "오늘의 북"

`portfolio.py`에 순수 함수를 추가한다.

```python
@dataclass(frozen=True)
class LimitUsage:
    code: str        # GROSS_EXPOSURE | LOSS_LIMIT | POSITION_NOTIONAL
    label: str
    used: Decimal
    limit: Decimal
    ratio: Decimal   # used / limit, 소수점 6자리 ROUND_HALF_UP
```

`BookSummaryOut`에 `limit_usage: list[LimitUsageOut]`와 `last_valued_at: str | None`을 추가한다. 후자는 `repository.py`에 `SELECT MAX(valued_at) FROM valuations` 질의를 더해 가져온다(SQL은 repository에만, 파라미터 바인딩 유지).

한도 값은 `config/market_conventions.yaml`의 `risk_limits`를 그대로 쓴다. **코드에 하드코딩하지 않는다.**

함께 고칠 것: `schemas.py`, `types.ts`, `METHODOLOGY.md`(사용률 산식과 손계산 예시), `tests/test_portfolio.py`.

**기존 26개 테스트는 계속 통과해야 한다.** 위 변경은 모두 필드 추가라 기존 계약을 깨지 않는다.

---

## 11. 금지 사항 점검표

| 항목 | 이 설계에서의 처리 |
|---|---|
| 엠대시·엔대시 | 화면 문구 0개. 빈 값 표시를 `—` → `-`로 교체(`format.ts` 5곳, CSS 주석 1곳). 음수 부호 `−`(U+2212)는 수학 기호라 유지 |
| eyebrow 라벨 | 홈 3개 섹션에 **0개**. 섹션 번호 라벨 없음 |
| 가운뎃점 | 메타데이터 줄당 최대 1개 (예: `2026-09-22 · pykrx`) |
| 장식 상태 점, 네온, 그라데이션 글자 | 없음 |
| 순수 `#000000` / `#ffffff` | 없음. `--canvas` `#0F1418`, `--ink`(다크) `#E4E9ED` |
| 강조색 | 1개(`--accent` 틸), 채도 76% / 46% |
| 빨강·파랑 | 손익·방향 의미색으로만. 강조색과 44°·177° 분리 |
| 카드 | 전표·명세 패널만(위계 있음). 북 현황 0개. 같은 카드 3개 나열 없음 |
| 3열 동일 카드 | 없음. 홈 증거 섹션은 세로 원장 구조 |
| 이모지 | 없음. 아이콘은 `@phosphor-icons/react` 한 가족, 굵기 통일 |
| 빈 수식어 | "혁신적인", "차세대", "원활한" 미사용. 버튼은 구체 동사 |
| 가짜 데이터·가짜 이름 | 실제 종목(005930 삼성전자)과 실제 계산값만 사용 |
| `picsum` 등 스톡 이미지 | 미사용. 삽화는 실제 UI 원자 |
| z-index 남발 | 4단계만, §6-5에 문서화 |
| 무한 반복 애니메이션 | 없음 |

---

## 12. 설치할 패키지

`package.json` 확인 완료 — 넷 다 미설치다. 레지스트리에서 버전을 확인했다.

```
npm install motion@13.4.0 @phosphor-icons/react@2.1.10 geist@1.7.2 @fontsource/ibm-plex-sans-kr@5.3.0
```

| 패키지 | 버전 | 용도 |
|---|---|---|
| `motion` | 13.4.0 | M1–M10. Framer Motion의 현재 패키지명, `motion/react`로 임포트 |
| `@phosphor-icons/react` | 2.1.10 | 아이콘 한 가족 |
| `geist` | 1.7.2 | Geist / Geist Mono woff2 원본 (Next 전용 엔트리는 쓰지 않고 `dist/fonts/**`의 가변 woff2만 `@font-face`로 참조) |
| `@fontsource/ibm-plex-sans-kr` | 5.3.0 | unicode-range 분할 한글 |

GSAP·Three.js·Tailwind·표 라이브러리는 설치하지 않는다.

---

## 13. 구현 시 유의점

- **CSS 특이도.** 직전 감사에서 `.ledger td`(0,1,1)가 `.ledger-formula`(0,1,0)를 이겨 `padding-right`가 무시되던 버그가 있었다. 새 시스템에서도 요소+클래스 특이도를 맞춰 쓰고, `!important`로 덮지 않는다.
- **모션 값과 정확성.** M2의 중간 프레임은 표시용 근사치다. 전환이 끝나면 반드시 백엔드 문자열을 그대로 렌더링한다. 프론트에서 Decimal 연산을 하지 않는다.
- **`.sr-only` 탈출.** 절대 위치 요소가 스크롤 컨테이너를 빠져나가 문서 폭을 늘리는 문제를 직전 패스에서 고쳤다. 새 표 구조에서도 스크롤 컨테이너에 `position: relative`를 유지한다.
- **터치 분기.** 화면 폭이 아니라 `@media (pointer: coarse)`로 타깃을 키운다.
