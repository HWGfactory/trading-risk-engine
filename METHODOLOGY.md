# 계산 방법론과 근거

이 문서의 손계산 예시는 `backend/tests/test_linear.py`의 기대값과 같다.
엔진을 고쳐서 값이 달라지면 이 문서를 먼저 고치고 테스트를 갱신한다.

## 1. 공통 원칙

| 항목 | 규칙 | 이유 |
|---|---|---|
| 숫자 타입 | Python `Decimal` | 이진 부동소수점(float)은 0.1 같은 값을 정확히 표현하지 못한다 |
| 비용(수수료·세금) | 원 미만 절사 | 국세·증권사 정산 관행 |
| 명목금액·손익 | 원 단위 반올림(ROUND_HALF_UP) | 주가·지수 × 승수는 대부분 정수로 떨어지며, 남는 경우만 반올림 |
| 수익률 | 소수 6자리 반올림 | 화면 표시는 % 둘째 자리 |
| 부호 | 매수(LONG) +1, 매도(SHORT) −1 | 손익과 순노출 계산에 공통 적용 |

## 2. 선형 상품 (주식·선물) 시가평가

기호: P₀ 진입가, P 평가가격, Q 수량, M 거래승수(주식 1), s 방향 부호, c 수수료율

- 진입 명목금액 = P₀ × Q × M
- 평가 명목금액 = P × Q × M
- 평가손익(비용 전) = s × (P − P₀) × Q × M
- 수수료 = 진입 명목 × c + 평가 명목 × c (청산 수수료는 평가가격으로 청산한다고 가정한 추정치)
- 순손익 = 평가손익 − (수수료 + 거래세)
- 명목 대비 수익률 = 순손익 ÷ 진입 명목금액
- 순노출(signed exposure) = s × 평가 명목금액, 총노출 = |순노출|의 합

### 거래세 (주식만)
- 근거: 증권거래세법 시행령 개정(2025년 세제개편안), 2026-01-01 이후 양도분부터 적용
- 코스피: 증권거래세 0.05% + 농어촌특별세 0.15% = 0.20%
- 코스닥: 증권거래세 0.20% (농특세 없음)
- 과세 대상은 매도 대금이다. 매수 포지션은 청산 매도 대금(평가가격 기준 추정),
  매도(공매도) 포지션은 진입 시점의 매도 대금에 부과한다.
- 증권거래세와 농특세는 각각 원 미만 절사한 뒤 더한다.
- 파생상품(선물·옵션)은 증권거래세 대상이 아니다.

### 선물 거래승수·호가단위
- 근거: KRX 파생상품 상품명세. 값은 `config/market_conventions.yaml`에 있으며 사용 전 KRX에서 재확인한다.
- 코스피200 선물 250,000 / 0.05pt, 미니 코스피200 선물 50,000 / 0.02pt,
  코스닥150 선물 10,000 / 0.1pt, 주식선물 10주
- 호가단위의 배수가 아닌 가격은 API에서 거부한다.

### 증거금 (선물, 입력 시)
- 개시증거금(진입 기준) = 진입 명목금액 × 증거금률
- 현재 필요증거금 = 평가 명목금액 × 증거금률
- 증거금 대비 수익률 = 순손익 ÷ 개시증거금
- 증거금률은 거래소·증권사 기준이 바뀌므로 사용자가 입력한다.

## 3. 손계산 예시

### 예시 A: 코스피 주식 매수, 수수료 0.015%
100주, 진입 70,000, 평가 75,000
- 진입 명목 7,000,000 / 평가 명목 7,500,000
- 평가손익 (75,000 − 70,000) × 100 = 500,000
- 수수료 7,000,000 × 0.015% = 1,050, 7,500,000 × 0.015% = 1,125
- 거래세 7,500,000 × 0.05% = 3,750, 7,500,000 × 0.15% = 11,250, 합계 15,000
- 비용 합계 17,175, 순손익 482,825, 수익률 482,825 ÷ 7,000,000 = 6.8975%

### 예시 B: 코스닥 주식 공매도, 수수료 없음
50주, 진입 20,000, 평가 18,500
- 평가손익 −1 × (18,500 − 20,000) × 50 = 75,000
- 거래세 진입 매도 대금 1,000,000 × 0.20% = 2,000
- 순손익 73,000, 순노출 −925,000

### 예시 C: 원 미만 절사
코스피 1주, 진입·평가 12,345, 수수료 0.015%
- 수수료 1.85175 → 1원, 두 번 = 2원
- 거래세 6.1725 → 6원, 18.5175 → 18원, 합계 24원
- 순손익 −26원

### 예시 D: 코스피200 선물 매수 2계약, 증거금률 10%
진입 350.00, 평가 352.50
- 진입 명목 350 × 2 × 250,000 = 175,000,000
- 평가손익 2.5 × 2 × 250,000 = 1,250,000, 거래세 0
- 개시증거금 17,500,000, 현재 필요증거금 17,625,000
- 증거금 대비 수익률 1,250,000 ÷ 17,500,000 = 7.1429%

## 4. 북 집계와 한도
- 총노출 = Σ|순노출|, 순노출 = Σ순노출, 순손익 = Σ순손익 (상품군별·전체)
- 한도 점검: 단일 포지션 명목금액, 북 총노출, 북 평가손실. 한도 값은 데모용 예시다.

### 한도 사용률
화면이 비율을 계산하지 않도록 엔진이 소수점 6자리까지 반올림해 내려준다(`portfolio.limit_usage`).

| 한도 | 소진액(used) | 산식 |
|---|---|---|
| 총노출 | Σ\|순노출\| | used ÷ `max_gross_exposure` |
| 손실 | 순손익이 음수일 때 그 절댓값, 이익이면 0 | used ÷ `max_loss` |
| 단일 포지션 | 열린 포지션 중 최대 \|순노출\| | used ÷ `max_position_notional` |

이익은 손실 한도를 소진하지 않는다. 한도 값은 `config/market_conventions.yaml`의 `risk_limits`에서만 읽는다.

**손계산 예시 E** (한도: 총노출 150,000,000 / 손실 1,000,000 / 단일 포지션 100,000,000)
- 주식 매수 100주 70,000 → 75,000, 코스피: 순노출 +7,500,000, 순손익 485,000
- 선물 매도 1계약 350 → 352, 승수 250,000: 순노출 −88,000,000, 순손익 −500,000
- 총노출 7,500,000 + 88,000,000 = 95,500,000 → 95,500,000 ÷ 150,000,000 = **0.636667**
- 북 순손익 485,000 − 500,000 = −15,000 (손실) → 15,000 ÷ 1,000,000 = **0.015000**
- 최대 단일 포지션 88,000,000 → 88,000,000 ÷ 100,000,000 = **0.880000**

### 마지막 평가 시각
열린 포지션에 대한 `valuations.valued_at`의 최댓값. 평가 이력이 없으면 값이 없다.

## 4-1. 근거 추적 (TraceStep.inputs)
계산 근거의 각 줄은 그 값이 **어떤 입력·설정값에서 나왔는지**를 함께 내려준다.
화면의 "근거 줄 ↔ 입력 칸 상호 강조"가 이 목록만 보고 동작하므로, 프론트는 의존 관계를 추측하지 않는다.

| 근거 줄 | inputs |
|---|---|
| 진입 명목금액 | `entry_price`, `quantity`, `multiplier` |
| 평가 명목금액 | `mark_price`, `quantity`, `multiplier` |
| 평가손익 | `entry_price`, `mark_price`, `quantity`, `multiplier`, `direction` |
| 수수료 | `entry_price`, `mark_price`, `quantity`, `multiplier`, `commission_rate` |
| 거래세 (주식) | 매수는 `mark_price`, 매도는 `entry_price` + `quantity`, `multiplier`, `direction`, `market`, `tax_rate` |
| 거래세 (선물) | `asset_class` |
| 순손익 · 명목 대비 수익률 | 위 단계에서 쓰인 입력 전부 (중복 없이, 등장 순서 유지) |
| 개시증거금 | `entry_price`, `quantity`, `multiplier`, `margin_rate` |
| 증거금 대비 수익률 | 위 전부 + `margin_rate` |

거래세의 의존 입력이 방향에 따라 갈리는 이유는 과세 대상이 매도 대금이기 때문이다.
매수 포지션은 청산 매도(평가가) 기준, 매도 포지션은 진입 매도(진입가) 기준으로 과세한다.

## 5. 데이터 무결성 장치
- 대사(reconciliation): 재평가 때마다 Python 엔진 집계와 SQL 뷰 `v_book_by_asset_class` 집계를 비교한다.
  한 포지션이라도 평가에 실패하면 이전 평가가 섞이므로 대사를 건너뛰고 그렇게 표시한다.
- 감사추적: `valuations`는 INSERT만 허용한다. 트리거가 UPDATE·DELETE를 막는다.
  각 평가에는 평가가격, 가격 출처, 가격 기준일, 재평가 배치 ID(run_id)가 남는다.
- 시세 출처: pykrx(네이버 수정주가) → 실패 시 FinanceDataReader. 최근 종가가 5일보다 오래되면
  거래정지 가능성을 화면에 경고한다.

## 6. 가정과 한계
- 청산 비용은 평가가격으로 지금 청산한다고 가정한 추정치다. 실제 체결가·슬리피지는 반영하지 않는다.
- 수정주가 기준이므로 진입 이후 기업행위가 있었다면 진입가를 수정 기준으로 환산해야 한다.
- 배당·대차수수료·자금조달비용(캐리)은 1단계 범위 밖이다.

## 7. 체결 기반 포지션과 실현손익

포지션은 직접 입력받는 값이 아니라 체결(trade)을 합쳐 만든 결과다.
체결이 들어올 때마다 포지션을 증분 갱신하며, 전체를 다시 계산하지 않는다.

### 7-1. 왜 이동평균법인가

실현손익은 **이동평균법(moving average)** 으로 계산한다. 선입선출(FIFO)이 아니다.

- 이 도구는 체결 하나하나가 아니라 **포지션 단위**로 리스크와 손익을 본다.
  이동평균법은 포지션에 단가 하나를 붙이므로 이 관점과 맞는다.
- FIFO는 남은 수량을 체결 단위로 계속 추적해야 한다(어느 매수분이 얼마나 남았는지).
  데스크 성과 측정에는 그만큼의 추적 부담을 질 이유가 크지 않다.
- 데스크 성과 측정과 내부 관리 회계에서 이동평균법이 널리 쓰인다.

**한계**: 세무 목적의 양도차익 계산에는 FIFO를 쓰는 경우가 있다.
이 도구의 실현손익은 **내부 성과 측정용**이며 세무 신고 수치가 아니다.

### 7-2. 세 규칙

수량은 절댓값으로 계산하고 방향은 부호로 관리한다(매수 +, 매도 −).

**규칙 1. 같은 방향 추가 체결 (증가)**
```
새 평균단가 = (기존 수량 × 기존 평균단가 + 추가 수량 × 체결가) ÷ 총수량
```
소수 4자리 반올림(ROUND_HALF_UP). 실현손익은 발생하지 않는다.

**규칙 2. 반대 방향 체결, 보유 수량 이내 (부분·전량 청산)**
평균단가는 **바뀌지 않는다.** 수량만 줄어든다.
```
실현손익(매수 포지션 청산) = (체결가 − 평균단가) × 청산수량 × 승수
실현손익(매도 포지션 청산) = (평균단가 − 체결가) × 청산수량 × 승수
```
원 단위 반올림 후 `positions.realized_pnl`에 누적 가산한다.
수량이 0이 되면 평균단가는 0, 상태는 CLOSED.

**규칙 3. 반대 방향 체결, 보유 수량 초과 (방향 전환)**
한 체결 안에서 두 단계가 순서대로 일어난다.
1. 보유 수량 전부를 규칙 2로 청산해 실현손익을 확정한다.
2. 남은 수량이 새 포지션이 되고, **평균단가는 이번 체결가**가 된다.

### 7-3. 비용 처리

- 각 체결의 수수료 = 체결금액 × 그 체결의 수수료율, **원 미만 절사**
- 거래세는 매도 체결에만, 주식에만 붙는다(파생 제외), **원 미만 절사**
- **청산 체결에서 발생한 비용은 실현손익에서 차감한다.**

#### 방향 전환 체결의 비용 안분과 잔여 1원

전환 체결은 청산 다리와 진입 다리로 쪼갠다. 그런데 각 다리의 체결금액에 대해 따로 절사하면
**두 조각의 합이 체결 전체 기준 금액보다 1원 작아지는 경우가 실제로 생긴다.**
표본 12개를 넣어 확인했더니 6개에서 차이가 났다.

```
예: 80,333원 × 150주 매도 (100주 보유 중), 수수료 0.015%
  전체 기준  round(80,333 × 150) × 0.015% = 12,049,950 × 0.015% = 1,807.49 → 절사 1,807
  청산 다리  round(80,333 × 100) × 0.015% =  8,033,300 × 0.015% = 1,204.99 → 절사 1,204
  진입 다리  round(80,333 ×  50) × 0.015% =  4,016,650 × 0.015% =   602.49 → 절사   602
  두 다리 합 1,806  ≠  전체 1,807      (1원 부족)
```

실제로 내는 돈은 **체결 전체에 대한 금액**이다. 그래서 순서를 이렇게 정했다.

1. 체결 전체 기준으로 수수료와 거래세를 먼저 계산한다.
2. 진입 다리를 제 체결금액 기준으로 계산한다.
3. **청산 다리 = 전체 − 진입 다리.** 남는 잔여(0원 또는 1원)를 청산 다리가 흡수한다.

**잔여를 청산 다리에 붙이는 이유**: 청산 다리의 비용은 실현손익에서 차감된다.
진입 쪽에 붙이면 그 1원이 다음 청산 때까지 미뤄지고, 전량 청산 후 다시 진입하지 않으면
어디에도 반영되지 않는다. 청산 쪽에 붙이면 지금 바로 반영된다.

이 규칙은 `test_flip_leg_costs_sum_to_whole_fill`에서 8개 케이스로 고정했다
(홀수 수량, 끝자리 3·7 가격, 1주 단위, 선물 승수가 곱해지는 경우).

#### 진입 비용을 이중 계상하지 않으면서 누락하지도 않는 방법

이 부분은 실제로 계산을 돌려 확인하고 정했다.

**이중 계상은 일어나지 않는다.** 실현손익은 "청산된 수량"을, 평가손익은 "남은 수량"을 다루므로
수량 기준으로 겹치지 않는다. 미청산 포지션에 대해서는 `linear.py`가 진입 수수료를 이미 반영한다.

**대신 반대 방향의 누락이 생긴다.** 청산 체결의 비용만 차감하면, 전량 청산된 종목의
진입 수수료가 어느 숫자에도 남지 않는다.

```
왕복: 100주 70,000 매수 → 100주 80,000 전량 매도 (수수료 0.015%, KOSPI)
  실제 경제적 손익 = 1,000,000 − 1,050 − 1,200 − 16,000 = 981,750
  청산 비용만 차감하면                                   = 982,800   (진입 수수료 1,050 누락)
```

그래서 `positions.entry_cost`에 **보유 수량에 대해 실제로 낸 진입 수수료**를 누적하고,
청산할 때 청산 비중만큼 안분해 실현손익에서 함께 차감한다.
```
안분액 = 보유분 진입 비용 × 청산수량 ÷ 보유수량   (원 단위 반올림)
실현손익(비용 후) = 실현손익(비용 전) − 청산 비용 − 안분액
```
남은 `entry_cost`는 그만큼 줄어든다. 전량 청산하면 0이 된다.

**여기서 빼는 진입 수수료는 "평균단가에 실린 비용을 청산 수량만큼 안분한 값"이다.**
어느 체결에서 낸 수수료인지 따지지 않는다. 이동평균법에서는 보유분이 이미 한 덩어리로
섞여 있어서, 그 덩어리에 붙어 있는 비용도 수량 비례로 나누는 것이 일관된 처리다.

**손계산 예시 L (시드 데이터의 삼성전자 케이스)**
```
진입 1  300주 @268,000  체결금액 80,400,000 × 0.015% = 12,060
진입 2  200주 @284,000  체결금액 56,800,000 × 0.015% =  8,520
                                  entry_cost 누적      = 20,580
평균단가 (300×268,000 + 200×284,000) ÷ 500 = 274,400

청산   100주 @281,000  체결금액 28,100,000
  수수료      × 0.015% =  4,215
  증권거래세  × 0.05%  = 14,050
  농특세      × 0.15%  = 42,150
                청산 비용 = 60,415
  진입 수수료 몫 20,580 × 100 ÷ 500 = 4,116

실현손익(비용 전) (281,000 − 274,400) × 100 = 660,000
실현손익(비용 후) 660,000 − 60,415 − 4,116  = 595,469
남은 entry_cost 20,580 − 4,116 = 16,464 (잔량 400주에 대한 몫)
```

**이동평균법이라서 이렇게 된다.** 선입선출(FIFO)로 계산하면 청산분에 실린 진입 수수료가
달라진다. 위 예에서 FIFO라면 청산된 100주는 먼저 산 300주(@268,000, 수수료 12,060) 중
일부이므로 진입 수수료 몫은 12,060 × 100 ÷ 300 = 4,020원이 되어 96원 차이가 난다.
청산 가격 기준(평균단가 274,400 대 FIFO 268,000)도 달라지므로 실현손익 자체가 바뀐다.
**어느 쪽이 맞고 틀린 것이 아니라 방식의 차이다.** 같은 포지션이라도 어떤 순서로
원가를 배분하느냐가 다를 뿐이고, 한 방식을 정해 일관되게 쓰는 것이 중요하다.
이 도구는 포지션 단위 성과 측정이 목적이라 이동평균법을 쓴다(7-1 참조).

평가손익 쪽도 맞춰 두었다. `LinearPosition.entry_commission_paid`에 실제 누적액을 넘기면
`linear.py`가 **추정하지 않고 실제 금액을 쓴다.** 값을 넘기지 않으면 예전처럼 추정한다.

### 7-4. 손계산 예시 (테스트 기대값의 출처)

**예시 F: 분할 매수 (규칙 1)**
- 100주 70,000 매수 → 평균단가 70,000, 수량 100
- 50주 76,000 추가 매수
- (100 × 70,000 + 50 × 76,000) ÷ 150 = 10,800,000 ÷ 150 = **72,000**, 수량 150

**예시 G: 부분 청산 (규칙 2)**
- 위 상태에서 60주를 80,000에 매도
- (80,000 − 72,000) × 60 × 1 = **480,000** (비용 전)
- 잔량 90, 평균단가 **72,000 유지**

**예시 H: 전량 청산 (규칙 2, 손실)**
- 남은 90주를 71,000에 매도
- (71,000 − 72,000) × 90 × 1 = **−90,000**
- 누적 실현손익 480,000 − 90,000 = **390,000**, 수량 0, 상태 CLOSED

**예시 I: 방향 전환 (규칙 3)**
- 매수 100주(평균 70,000) 보유 중 150주를 75,000에 매도
- 청산분 (75,000 − 70,000) × 100 = **500,000**
- 남은 50주는 **매도 포지션, 평균단가 75,000**

**예시 J: 선물 거래승수**
- 코스피200 선물 2계약 350.00 매수 → 1계약 352.50 매도
- (352.50 − 350.00) × 1 × 250,000 = **625,000**
- 남은 1계약의 평균단가는 350.00 유지. 파생이라 거래세는 0

**예시 K: 비용 차감과 원 미만 절사**
- 평균 72,000·150주 중 60주를 80,333에 매도, 수수료 0.015%, 코스피
```
체결금액   80,333 × 60          = 4,819,980
수수료     × 0.015%  =   722.997  → 절사   722
증권거래세 × 0.05%   = 2,409.99   → 절사 2,409
농특세     × 0.15%   = 7,229.97   → 절사 7,229
                       비용 합계 = 10,360
실현손익(비용 전) (80,333 − 72,000) × 60 = 499,980
실현손익(비용 후) 499,980 − 10,360        = 489,620
```

### 7-5. 일별 스냅샷

`snapshot_date`는 **Asia/Seoul 기준**으로 파이썬에서 계산한다.
SQLite의 `datetime('now')`는 UTC라서 한국 저녁에 찍으면 전날 날짜로 기록되기 때문이다.

같은 날 다시 찍으면 **409로 거절**한다. 덮어쓰지 않는다.
다시 찍어야 하면 정정본(`revision` +1)을 **새 행으로 추가**한다. 이전 행은 그대로 남는다.
조회는 날짜·종목별 최신 정정본(`v_latest_snapshot`)을 쓴다.

### 7-6. 청산 API의 동작 변경

기존 `POST /api/positions/{id}/close`는 상태만 CLOSED로 바꿨다.
지금은 **반대 방향 전량 체결을 한 건 쌓는다.** 체결 이력이 남고 실현손익이 확정된다.
평가가격이 필요하므로 최근 평가가 없으면 409로 거절한다.

보유 수량을 넘는 반대 방향 체결은 거절하지 않는다. 규칙 3의 방향 전환으로 해석한다.
이 앱은 공매도 포지션을 지원하므로 그 해석이 맞다.
